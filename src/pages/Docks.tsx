import { Box, Typography } from '@mui/material';

export default function Docks() {
  return (
    <Box data-testid="docks-page">
      <Typography variant="h3">Docks</Typography>
      <Typography>
        Here I can write anything what I want and nobody can stop me (except law
        and God).
        <br />
        But God not exist, so only law can stop me))
      </Typography>
    </Box>
  );
}
